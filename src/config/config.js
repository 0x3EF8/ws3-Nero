module.exports = {
    appState: require('./appstate.json'),
  
    options: {
      listenEvents: true,
      selfListen: false,
      // Optionally add experimental features:
      randomUserAgent: false,
      bypassRegion: 'PH' 
    }
  };
  